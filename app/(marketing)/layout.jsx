// ============================================================================
// MARKETING LAYOUT — No AppShell, no sidebar, no auth required
// This layout renders the public-facing landing page and any future
// marketing routes (blog, pricing, etc.) without the app chrome.
// ============================================================================

export default function MarketingLayout({ children }) {
  return <>{children}</>;
}
